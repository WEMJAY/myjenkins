pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh '''
          echo "Hello from Jenkins Pipeline!"
          echo "Running on: $(hostname)"
          echo "User: $(whoami)"
          date
        '''
      }
    }
  }
}

